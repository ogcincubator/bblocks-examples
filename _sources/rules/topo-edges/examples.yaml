## Prefixes will be used for all RDF Turtle resources so that they can be omitted from the actual snippets
# prefixes:
#   dct: http://purl.org/dc/terms/

## List of examples
examples:

  - title: Line and points

    ## Markdown content that will be shown for the example.
    content:
      Simple line using two points, which must be present according to inherited rules.

    ## Base URI for semantic uplift
    base-uri: http://example.com/nodes/


    ## List of code snippets for this example
    snippets:
      - language: json
        # Reference to file
        ref: examples/nodes.json

