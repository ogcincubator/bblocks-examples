## Forest Ontology

Defines a set of properties for a forest typology uploaded directly to an official register.

Uses samples from the a specific project (FOCAL)

> Note these URIs may be mapped to properties in **any** JSON schema.
